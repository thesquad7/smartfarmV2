{
    "private": true,
    "scripts": {
        "build": "css-minify -f public/css/main.css -o public/css/css-dist/",
        "metrics": "php ./vendor/bin/phpmetrics --config=phpmetrics-config.yml",
        "dev": "npm run development",
        "development": "mix",
        "watch": "mix watch",
        "watch-poll": "mix watch -- --watch-options-poll=1000",
        "hot": "mix watch --hot",
        "prod": "npm run production",
        "production": "mix --production"
    },
    "devDependencies": {
        "axios": "^0.25",
        "laravel-mix": "^6.0.6",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14"
    }
}
